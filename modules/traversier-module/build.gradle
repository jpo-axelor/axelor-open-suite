apply plugin: "com.axelor.app-module"

apply {
    version = rootProject.file("version.txt").text.trim()
}

axelor {
    title "Traversier Module"
}

dependencies {
    compile project(":modules:axelor-timecard")
}

